Steps :
- App GUI avec python (tkinter)
- Initialiser et relier db
- Utiliser le un outil de chiffrage/dechiffrage (Frenet ?)

Fonctions de l'app :
- Se connecter avec un mdp
- Fonction de recherche + affichage de l'username, email et mdp et du site
- Ajouter des mdp
- Modifier des mdp
- Supprimer des comptes


Bonuses :
- clip to clipboard
- lock script at the end (cf below)
- improve design
- improve optimisation (windows repeating etc)
- Open private key from txt file (cf below)



69_G0FUCKY0URS3LF_70



OPEN FILE :

filename = askopenfilename(title="Ouvrir votre document",filetypes=[('txt files','.txt'),('all files','.*')])
fichier = open(filename, "r")
content = fichier.read()
fichier.close()

Label(fenetre, text=content).pack(padx=10, pady=10)





LOCK SCRIPT :

import py_compile

script = "C:\\temp\\myscript.py"
py_compile.compile(script)
